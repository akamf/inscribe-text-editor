#include "FileManager/FileManager.h"
#include "Utils/Logging/Logger.h"

#include <iostream>

// int changed = 0;
// char filename[256] = "";

int main() {
    FileManager fileManager;
    
    std::cin.get();

    return 0;
};
